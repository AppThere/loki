# Open questions, indexed by the instrument that would settle them.
#
# Why this file exists
# --------------------
# L9-019's third clause requires every open finding to record **what would
# settle it**. That is already a register of pending questions keyed by their
# instrument — what it lacked was a trigger. Nothing prompted anyone to re-read
# the register when an instrument finally landed, so a decision taken while a
# question was open could stand for three commits after the counter that
# condemned it existed (Spec 08 r55/r56: the macOS total-only probe shipped in
# r53, the regime sweep that refuted it landed in r55, and nothing connected
# them until a human did).
#
# This is that trigger. Each row names a symbol or path that does not exist yet;
# the gate fails the moment it does, with the question attached. The failure is
# the prompt to ask **"what did I decide while this was open?"** — not just to
# answer the question going forward.
#
# Format
# ------
#   <state><TAB><path glob><TAB><instrument regex><TAB><question><TAB><on-fire>
#
# **The last field is required, and that is the point.** A row can name an
# instrument and still be useless when it fires months later: "the counter you
# were waiting for exists" tells the reader nothing about what to go and re-read.
# The content of that text is judgement and cannot be mechanised — but a *thin*
# row can be made unwritable, which is the program's usual move (L08-043). The
# gate rejects a row with fewer than five fields, so the sweep instructions are
# written while the question is fresh and the person holding the context is
# still here.
#
# on-fire answers exactly one question: **what did I decide while this was
# open, and where would that decision now be wrong?**
#
# The path glob scopes where the instrument would be *built*, which is what
# makes the row falsifiable: a comment naming `host_statistics64` is a statement
# that it is missing, and matching it would fire the gate on the very prose that
# records the gap. Comment lines are skipped for the same reason.
#
# state is one of:
#   awaiting   the instrument does not exist; the gate fails if it appears
#   landed     the instrument exists and the sweep has been done; kept as a record
#
# So resolving a row means doing the sweep and moving it to `landed` with the
# outcome written into the question text. Rows are not deleted: an answered
# question is evidence about how the answer was reached.
#
# Usage: scripts/check-pending-questions.py

landed	appthere-ui/src/device_probe*.rs	host_statistics64|GlobalMemoryStatusEx|sysinfo::	Cross-platform comparability of the "available RAM" figure (Spec 08 r56, swept r58 when `sysinfo` landed — the first firing of this gate, on the question it was seeded with).	SWEEP OUTCOME: (a) the comparability question stays OPEN and is now *collectable* — `available_permille_of_total` fires on all three platforms from the next run, where before it was mute on the two in question; it is tracked as I-25 rather than here, because a register row is a trigger and not a home. (b) Decisions taken while the platforms were total-only, re-read: the r53 macOS total-only choice is SUPERSEDED (its reasoning held, its destination did not); the r53 claim that the fallback "changes the source and not the number" was true only at 8 GiB and is not a general adequacy claim; Phase 2's close criteria were split in r57 so no Phase 2 claim now rests on the fallback being adequate. (c) NOT re-opened: the budget derivation itself, which never assumed a platform — it consumes whichever figure arrives, which is why the fix was a probe change and not a derivation change.
awaiting	loki-renderer/src/*.rs	frame_time_ms|frame_ms|FrameTimer	Is page re-rasterisation perceptible (Spec 08 r54/r55)? scene_build_ms + gpu_submit_ms is a LOWER BOUND — submission, not completion — so it cannot answer this alone.	ARRIVAL RE-OPENS: whether the mount window needs retention hysteresis at all, which was left undecided pending exactly this measurement.
awaiting	appthere-ui/src/device_profile*.rs	font_scale|FontScale	The platform accessibility text scale reaching DeviceProfile (Spec 08 I-24).	ARRIVAL RE-OPENS: the popover reachability table's 1.5x/2.0x rows, which are marked "only once I-24 lands" and become present-tense; MENU_ROW_HEIGHT_PX, which equals TOUCH_MIN only while rows are device-fixed; and the token split by purpose, whose max(text-relative, device-fixed) rule has never been exercised.
landed	appthere-ui/src/components/ribbon/*.rs	AtPopoverHost|use_popover_anchor	The ribbon overflow menu still uses the render-in-place + root-backdrop split, which does not work in this engine: its z-index 41 is deep inside `Router` while `AtBackdropHost`'s backdrop is a root sibling at 40, so the backdrop is hit first and the menu's controls are dead while it is raised (Spec 08 r66). Hosting it in `AtPopoverHost` is the fix.	SWEEP OUTCOME (r91, I-28): the menu is hosted; `scripts/sitting/run.sh ribbonoverflow` is the regression, and its assertion is the focus ring landing on a control INSIDE the menu — the patched shell focuses whatever the pointer hit, so a ring there is proof the hit test reached it, which is exactly what the root backdrop prevented. (a) **Every 'a higher z-index keeps it above the backdrop' claim is GONE, because the module carrying them is.** `overlay`'s backdrop mechanism — `use_provide_backdrop`/`AtBackdropHost`/`use_backdrop` — existed to let a consumer render its popup in place and borrow a root backdrop, which cannot work here, as the module itself said. The overflow menu was its last requester; after the migration the three apps still MOUNTED the host but nothing ever asked it for anything. A public API documented as non-functional with no callers would hand the next consumer the same defect, so it is deleted (L08-043) rather than warned about. `BACKDROP_Z_INDEX` survives — the popover host and the band gate both need the number. (b) **The band is still the right shape and its reason is unchanged.** The retraction of r64's account was already recorded on the constant and in the gate; nothing in this migration bears on it. What changed is that the band now has exactly one occupant family (the popover host's two layers) instead of two, which makes 'application crates may not enter it' easier to hold, not harder. (c) **API STRAIN, reported rather than absorbed, as I-28 asked.** Two findings. `Role::Panel` is the right role for a container of buttons — arrows/Enter/Space pass through to the focused one — but `route_key(Panel, Tab)` is `FocusNextControl`, which has NO performer anywhere in the workspace: `set_focus` is a `bool`, the same blocker as `AdvanceFocusPastAnchor`. This is its first real consumer and it arrives to find the action unimplementable; `on_key` dismisses on it, because a key consumed and dropped traps the keyboard in a menu it cannot leave. Recorded on the `advance_focus_past|focus_next_node` row, which now has two waiting consumers. And `AtRibbonIconButton` had no way to report its mounted handle, which every popover trigger needs for focus restore and placement — it gained an optional `on_mounted`.
awaiting	appthere-ui/src/components/popover/*.rs :(exclude)appthere-ui/src/components/popover/*_tests.rs :(exclude)appthere-ui/src/components/popover/interaction_dismiss.rs	DismissCause::PresentationChanged	A **producer** for `DismissCause::PresentationChanged`, which has had none since r68. Its arrival IS a modal fallback being implemented (T5.2's colour picker is the likely first — SV square, hue strip and fields are tall), so this row fires on the construction rather than on a predicted instrument path: the variant is parked, and any code outside its defining module and its tests that builds one is the un-parking (Spec 08 r70, L08-050).	ARRIVAL RE-OPENS: `PresentationChanged` gains a producer, so re-read whether `focus_after_dismiss` still returns `Anchor` for it once a real modal exists — the parked answer assumed the anchor survives, which a full-screen form may not preserve. Also re-read `interaction_anchor`'s r68 decision that a shrinking viewport repositions: that was decided in a world with ONE form, and a second form re-opens the transform-versus-dismiss question the r48 argument was actually about.
awaiting	appthere-ui/src/components/**/*.rs loki-text/src/**/*.rs	AtPanelHost *\{	A consumer mounting `AtPanelHost` — ADR-0013's own prescribed host, which has **zero mount sites** as of the r73 audit (only its own doc examples). An ADR naming a type with no call sites is a decision that was never adopted, whatever the prose says, and it misleads anyone reading ADRs to learn the conventions. Recorded under I-27 rather than as new work.	ARRIVAL RE-OPENS: I-27's count (seven ADR-0013 violations) — each was justified partly by there being no adopted host to migrate to, so the first adoption changes the cost of the other six. Also re-read whether `AtPanelHost`'s Compact-sheet vs Expanded-side-panel posture still matches what the adopting panel needs, since that posture was designed against no consumer.
landed	appthere-ui/src/components/popover/*.rs appthere-ui/src/components/home_tab/*.rs :(exclude)*_tests.rs :(exclude)appthere-ui/src/components/popover/interaction.rs	route_key\(	A caller for `route_key` — T4.5, the keyboard and focus work Phase 4 could not close without. The decision is fully specified, exhaustively matched and tested, and has zero call sites; `focus_after_dismiss` and `dismiss_sequence` are in the same state (Spec 08 r77). (The instrument is a *call* outside the defining module: the first draft matched `route_key(`'s own definition and an unrelated editor `onkeydown`, and fired on the day it was written — a row that reports arrival before the thing arrives is the register's own blind spot, L08-047.)	SWEEP OUTCOME (r78, T4.5): (a) `route_key` is dispatched from `AtPopoverHost`'s `onkeydown`, behind `key_event::key_from_parts` — the platform-to-vocabulary step, which is where Space arrives as Character(" ") and Shift+Tab as Tab+modifier, and where a mistake would have made `route_key` correct and unreachable. Three consumers route: the Recent menu, the spelling menu, the Open tooltip. (b) `Role` DID need a third variant, exactly as this row predicted. The tooltip was filed under `Panel`, and dispatch made the degenerate model an active one: `route_key(Panel, Tab)` is `FocusNextControl`, which CONSUMES the key and hands it to an `on_key` a tooltip does not supply — so Tab went nowhere while a tooltip showed — and the host's `autofocus` pulled focus out of whatever the pointer's owner was typing in. `Role::Tooltip` passes everything through (Escape included) and `Role::takes_focus` gates the `autofocus`. The r63 `host.rs` decision that "a tooltip is not a Role" is RETRACTED in place: its premise (the keyboard model is degenerate) was right, its conclusion (so give it no Role) was backwards, because every overlay the host renders reaches `route_key` whatever the request names. (c) `DismissCause::TabOut` has its first producer and `focus_after_dismiss` still returns `PastAnchor` — the dispatcher agrees on the TARGET and this stack cannot REACH it: `AdvanceFocusPastAnchor` needs "focus the node after this one", which `set_focus(bool)` cannot express, so `focus::focus_op` reports it as `Unsupported(PastAnchor)` and degrades to focusing the anchor (one extra Tab, not an arbitrary landing). Marked, not silently dropped — see the new `popover-focus-advance` row. (d) Focus itself was not merely unwired: `RenderedElementBacking::set_focus` defaults to `NotSupported` and the vendored backing did not override it, so NO component in this workspace could move focus. Fixed at the layer that owned it (`MountedBackend::focus_node` -> `BaseDocument::set_focus_to` / `clear_focus`), documented in docs/patches.md.
awaiting	appthere-ui/src/components/popover/*.rs :(exclude)*_tests.rs	advance_focus_past|focus_next_node	A mechanism for `DismissStep::AdvanceFocusPastAnchor` (Spec 08 r78). Tab out of a menu asks for focus to continue PAST the trigger, and `RenderedElementBacking::set_focus` is a `bool` — focus or blur — so the step cannot be expressed. blitz-dom's `BaseDocument::focus_next_node` would serve it, but reaching it means `appthere-ui` depending on the renderer crate. Until then `focus::focus_op` returns `Unsupported(PastAnchor)` and degrades to focusing the anchor, which costs the user one extra Tab and is logged rather than silent (L08-050). **A SECOND CONSUMER (r91, I-28).** The ribbon overflow menu migrated to `AtPopoverHost` with `Role::Panel` — the role that describes a container of buttons, passing arrows/Enter/Space to whichever has focus — and `route_key(Panel, Tab)` is `FocusNextControl`, which has no performer anywhere in the workspace. Same blocker, same `bool`. Its `on_key` dismisses on both focus-moves rather than dropping them, because a key consumed and dropped traps the keyboard inside a menu it cannot leave; `tab_is_a_focus_move_this_content_cannot_perform` records that as a test.	ARRIVAL RE-OPENS: `focus_tests::exactly_two_steps_are_unsupported_and_these_are_they` must lose the `PastAnchor` half — it is written as equality precisely so the deferral cannot outlive its reason. Also re-read whether `FallbackToAnchorContainer` is still unreachable: it fires when the trigger reported no mounted handle, and whatever mechanism lands here may also supply the container. ALSO RE-OPENS: `ribbon::overflow_menu`'s `on_key`, which dismisses on `FocusNextControl`/`FocusPrevControl` as the least-bad degradation. With a real performer those two stop being a dismissal and become what they say — and the choice between them and `Role::Menu` for that content is worth re-reading, since half the reason for `Panel` was that its Tab could at least be degraded honestly.
awaiting	appthere-ui/src/focus_ring.rs	focus-visible	`:focus-visible` support in this engine (Spec 08 r79). The focus ring is a plain `:focus`, so it also appears after a mouse click — the presentation every browser-targeted design system avoids. Established by runtime probe with the `:focus-visible` rule ordered LAST so it would have won if it applied; `:focus` still painted. Not worked around in app code: a "was the last input a key" flag puts an accessibility guarantee behind bookkeeping that can drift, and a missing ring is worse than an extra one.	ARRIVAL RE-OPENS: `focus_ring_css` switches selector and its COMPAT note retires — but re-probe rather than trusting a changelog, since the failure here is silent (an unmatched selector paints nothing, which is exactly what no-focus-indicator looked like before r79). Also re-read FOCUS_RING_WIDTH_PX: 3px was chosen partly because the ring must read at tablet viewing distance AND survive being shown on every click; only the second reason goes away.
landed	loki-text/src/routes/editor/*.rs appthere-ui/src/components/**/*.rs :(exclude)*_tests.rs	ZoomAnchor::Viewport	A **producer** for `ZoomAnchor::Viewport` (Spec 08 T5.6, r84). Pointer-anchored zoom is specified for Ctrl+scroll and pinch, and neither gesture could reach the app: `blitz-shell` handled the wheel entirely itself — scrolling nodes directly — and `blitz-traits` has no wheel `DomEventData` variant, so `onwheel` never fired. The variant was therefore parked, not forgotten: the arithmetic was written and tested at an arbitrary anchor, and only the trigger was missing. Any construction outside the defining module and its tests is the un-parking.	SWEEP OUTCOME (r88, T5.6): the producer is `editor_wheel_zoom::make_wheel_handler`, reached through a three-crate wheel chain built for it (`Document::handle_wheel` in blitz-dom, report-then-decline in blitz-shell's `MouseWheel` arm, `wheel` dispatch in dioxus-native-dom; all in docs/patches.md). (a) **The centre-anchored choice for the control STANDS, and half its reason is gone.** It rested on two arguments — that the reader pressing a status-bar button is looking at the page rather than at the control, and that centre was the only anchor reachable. The second is now false. The first is not, and a caret anchor is still not a drop-in: the caret may be off-screen (the reader scrolled away without moving it), so adopting it needs a caret-visible test with a centre fallback, which is a decision and not a substitution. Recorded rather than taken. (b) **`TODO(t5.6-effective-anchor)` stands and is NOT established as reachable.** The attempt to observe it was made — `LOKI_TEXTURE_CEILING_MB=16` to force the capability cap, then Ctrl+wheel past it — and the instrument could not reach the hazard: the page-fit rule (Spec 03 M2) flips the editor to the reflow renderer before the cap engages at this window size, and `capability_permille` reads `paginated_layout`, so in reflow there is no cap at all. What would settle it is a configuration where the cap bites while still paginated — a large page in a narrow viewport, or `view_mode_user_set` frozen to Paginated. (c) **Two defects the sitting caught that no unit test could.** `element_coordinates()` is relative to the event's *target*, not to the element whose handler runs, so the first anchor was out by 135 px — fixed by `BaseDocument::scrollport_origin`, carried on `NativeWheelData::scrollport`. And `ViewportController::zoom_to` read the metrics signal, which lags its own commands: two wheel events 0.2 ms apart both read `scroll_top = 200.0` (measured, not inferred), so the zoom advanced twice while the scroll moved once — fixed by `controller_offset::effective_offset`. (d) **`anchored_scroll` was missing the unscaled content offset.** Its model was `a = d·z − s`; a scroll container's padding does not scale, so it is `a = p + d·z − s`. The omission drifts by `p·(1 − r)`: 5 px per wheel notch and 120 px across the control's range at this app's 24 px padding. Predicted −5.04, measured −5, then 0 after the fix. This affected the CONTROL too, not only the wheel — every zoom taken since T5.6 landed was anchored by that much.
awaiting	loki-ooxml/src/**/*.rs loki-odf/src/**/*.rs :(exclude)*_tests.rs	ThemeColor::new	A **producer** for `ThemeColor` (Spec 08 T5.2, r87). `DocumentColor::Theme` and `resolve_rgb(&ThemeColor)` have existed since the model was written, and nothing constructs a `ThemeColor` outside `loki-primitives`' own tests: no importer reads a theme part, and the workspace's only `clrScheme` is a hardcoded string the PPTX *exporter* writes. So the picker's theme-swatch group is not deferred by preference — it has nothing to show, and a group showing a fixed palette as "the document's theme" would be a lie.	ARRIVAL RE-OPENS: the picker's document-colour group, which today omits theme colours because `to_hex` returns `None` for them — with a real palette they become resolvable and belong in it. Also re-read `editor_doc_colors`'s choice to define document colours as the style catalog: a theme palette is a *second* source of colours the document uses, and the two groups then need separating rather than merging.
